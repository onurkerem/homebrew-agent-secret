Homebrew Tap for agent-secret
================================

This repository contains the Homebrew formula for [agent-secret](https://github.com/onurkerem/agent-secret).

## Installation

```bash
brew tap onurkerem/agent-secret
brew install agent-secret
```

